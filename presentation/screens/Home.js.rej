diff a/presentation/screens/Home.js b/presentation/screens/Home.js	(rejected hunks)
@@ -5,11 +5,12 @@ import { Deck } from "../components/Deck";
 import { addDeck, getDecks } from "../../data/api";
 import { styles } from "../styles/Home";
 
+export const initialState = {
+  decks: {},
+  title: ""
+};
 export class Home extends Component {
-  state = {
-    decks: {},
-    title: ""
-  };
+  state = initialState;
   async componentDidMount() {
     const decks = await getDecks();
     this.setState({ decks });
@@ -24,6 +25,7 @@ export class Home extends Component {
     });
     this.props.navigation.navigate("Home");
   };
+  addDeck = deck => {};
   render() {
     const { navigation } = this.props;
     const { decks, title } = this.state;
