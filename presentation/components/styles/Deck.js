import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "center",
    height: "100%",
    borderWidth: 1
  },
  title: {
    padding: 5,
    fontWeight: "bold",
    fontSize: 48,
    textAlign: "center"
  },
  cards: {
    padding: 5,
    fontSize: 24
  }
});
